// Emberfire server — placeholder. The real game server is a future from-scratch implementation
// against the reconstructed wire protocol in shared/ (see docs/SERVER_BACKEND.md for the spec
// recovered from the leaked server binary). For now this just proves the target wires up.
#include <cstdio>

int main()
{
    std::puts("Emberfire server: not implemented yet. See docs/SERVER_BACKEND.md.");
    return 0;
}
