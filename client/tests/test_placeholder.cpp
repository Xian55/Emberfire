// Scaffold. See client/tests/CMakeLists.txt for why client coverage is thin (app coupling).
// Replace/extend as pure logic is extracted out of the SFML/WinMain client.
#include <gtest/gtest.h>

TEST(ClientScaffold, BuildsAndRuns) {
    EXPECT_TRUE(true);
}
